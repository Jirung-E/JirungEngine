#include "Collider.hpp"

using namespace JirungEngine;


Collider::Collider(const Point& start_point, const Vector2D& vector/*, Object* parent*/) : start_point { start_point }, vector { vector } { //, parent { parent } {
    
}